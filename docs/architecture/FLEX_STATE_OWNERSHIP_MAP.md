# FLEX State Ownership Map — Increment 1

> **Increment:** 1 — Baseline + Architecture Truth (no refactor in this increment)
> **Rule:** one authoritative frontend owner per domain (§7 single-owner rule). Route store + context + local copy + island copy of the same truth is the anti-pattern.

| Domain | Authoritative owner | Consumers | Derived / selectors | Transport | Persistence | Tenant scope | Known duplicates / gaps | Risk |
|---|---|---|---|---|---|---|---|---|
| **active call / call lifecycle / timer / conference** | `features/agent-workspace/state/mock-workspace-state.ts` — `MockWorkspaceState` singleton `workspaceState` | `useWorkspaceState()` (`state/use-workspace-state.ts`), `AgentWorkspacePage`, `CallManager` surfaces (`Idle/Incoming/Active/WrapUp`), `Dynamic Island` (`flex-call-island.tsx`), `Agent Assist launcher`, conference presentation | `callState`, `activeCall`, `isMuted`, `isOnHold`, `transfer`, `connection`, `wrapUpStartedAt`, `useWrapUpCountdown`, `SessionTimer` (1 Hz), `useCallTimer` | deterministic `setTimeout` schedule (`WORKSPACE_TIMINGS` 450–12000 ms), no WebSocket/SDK — mock adapter behind production interface | **none** (singleton in-memory; call survives route leave) | **NOT tenant-scoped** — call is per-agent, but `TenantContext` does not invalidate it; GAP | `AgentWorkspacePage` clones history via `useState` snapshot; Dynamic Island **observes/presents/navigates** (does not own timer/assist/conference) — violation would be P0 | **P0** — call desync / duplicate command path |
| **agent availability state** | same `MockWorkspaceState` (`agentState`, `agentStatePending`, `sessionStartedAt`) | `useWorkspaceState`, `agent-state-control.tsx`, `AgentDashboard` sections | `agentState`, `pending` flag | `WORKSPACE_TIMINGS.agentStateTransitionMs` 450 ms | none | not tenant-scoped | — | P0 |
| **Agent Assist session** | `features/agent-workspace/agent-assist/*` + `features/ai/*` (POC) — call-scoped, `mock-workspace-state` call lifecycle drives session | `AgentAssistPanel` (`agent-assist-panel.tsx`, `ASSIST_PANEL_META`) | `isAssistEligible` (call-scoped), `assistState` | mock repository `ai-repository.ts` / `ai-types.ts` — no realtime transcript stream at baseline | none — no permanent idle owner (correct) | not tenant-scoped | Transcript persistence must never be added (privacy §14) | P1 |
| **conference state / participants** | **telephony-owned** — `MockWorkspaceState` / `ActiveCall` (telephony boundary preserved) | conference presentation surfaces | `activeCall.conference` | mock sets | none | — | No separate UI-owned participant truth at baseline (correct). `CONFERENCE_*_AUDIT.md` notes unsupported capability — no fabricated diarization. | P1 |
| **tenant context** | `features/tenants/tenant-context.tsx` — `TenantContextProvider` (`{mode:'platform'} | {mode:'tenant', tenant}`) | `TenantContextIndicator`, tenants directory, platform surfaces | `context.mode`, `context.tenant` | in-memory `useState` only | **owner IS tenant** but **does NOT scope consumers** (no invalidation) | Every `domain/*Repository` operates on tenant-agnostic in-memory sets — see `FLEX_REALTIME_CHANNEL_AUDIT.md` | **P0** — wrong-tenant flashback if cache reappears |
| **current user / roles / permissions** | `auth/capabilities.tsx` — `CapabilityProvider` (`Role = 'super-admin'|admin|agent`) | `PrimaryRail`, `ContextSidebar`, `GlobalSearch`, `flex-profile-menu.tsx`, `has(capability)` guards, `/admin/roles` | `role`, `has(capability)`, `ROLE_CAPABILITIES`, `NAVIGATION` single model | `localStorage:flex.poc.role` (POC) | `localStorage` | not tenant-scoped | Role-string scatter is avoided (canonical `has()`), but **backend authority is DEFERRED** — UI visibility ≠ security | **P0** |
| **Social inbox / unread** | `features/social/social-repository.ts` (`socialRepository`) + `features/social/use-social-workspace.ts` — singleton in-memory `SocialInboxData` / `getMessages` | `SocialWorkspacePage`, `social-workspace-page.tsx` (list/detail/context rail), `conversation-list.tsx` | `conversations`, `getMessages(conversationId)`, `unreadCount`, `followUp/escalated` flags | deterministic mock; **no polling/SSE** — mutations are synchronous `sendReply/setFollowUp/escalate` + `setData(getInbox())` | in-memory | **NOT tenant-scoped** | Per-conversation selected state `activeId` is local `useState` in page (correct locality for route selection) | P1 — realtime dedupe/order must land in Inc.3 |
| **notifications** | Inertia flash + `hooks/use-flash-toast.ts` → `sonner` | `AppProviders` + `FlexAppShell` | `flash.success/error` | Inertia `flash` prop (`usePage`) | ephemeral | — | — | P2 |
| **queue realtime** | `data/*.mock` via `domain/*` (queue health) consumed by `features/dashboard/*` + `features/agent-dashboard/sections/queue-pressure.tsx` | `ContactCenterDashboard` (`queue-health.tsx`), `AgentDashboard` | `QueueHealth` fields | **polling only** (`dashboard-context.tsx` 5s) — see Realtime audit | none | not tenant-scoped | Duplicate intervals would be proven via `FLEX_REALTIME_CHANNEL_AUDIT.md` | P1 |
| **dashboard realtime** | `features/dashboard/dashboard-context.tsx` — `DashboardProvider` | `ContactCenterDashboardContent` (`active-calls`, `agent-wallboard`, `queue-health`, `traffic-chart`, `operations-summary`) | `data`, `isRefreshing`, `connectionState`, `lastUpdated` | `fetchDashboardData` + 5s poll + 5s stale check + `visibilitychange` | none | not tenant-scoped | Dashboard is the only consumer — no broadcast context over-sharing at baseline | P1 |
| **Agent Monitoring** | `features/agent-monitoring/use-agent-monitoring.ts` — `filteredAgents` + roster/columns/summary | `AgentMonitoringPage` (`agent-monitoring-roster.tsx`, `agent-state-summary.tsx`) | `filteredAgents` (Agent/Ext/Queue/State/State Time/Current Call/Calls Today/AHT), `stateSince` ticking via `useStateTimer` 1 Hz | mock repository + 1 Hz timer | none | not tenant-scoped | Row update recalculation on every tick is the perf hotspot (§13) | P1 |
| **route filters / selections** | Per-page `useState` (CDR `query`, campaigns `filters`, reports `query`, social `activeId`, tenants `search/status`) | local to page | — | none | `useState` only; dashboard `lastUpdated` not persisted | not tenant-scoped | Correct locality — only promote to shared owner if cross-route reuse proves needed (avoid mega-store §26) | P3 |
| **iframe integration (External CRM)** | `features/agent-workspace/integration/crm-integration-host.tsx` — boundary preserved, origin assumptions unchanged | `AgentWorkspacePage` (CRM host slot) | `integrationState` | `iframe` load + postMessage boundary | none | — | Unrelated state updates must not remount iframe (perf §13, privacy §14) | P1 |

## Single-owner rule — conformance at baseline

* **Correct:** `workspaceState` singleton → `useWorkspaceState` subscribe → many consumers (CallManager + Island + Assist). `TenantContextProvider` single context. `CapabilityProvider` single navigation + guard model. `DashboardProvider` single polling owner. `socialRepository` singleton + `useSocialWorkspace` binding.
* **Anti-pattern to avoid (not present at baseline):** route store + context + local copy + Island copy of the same call truth; separate UI-owned `Assist` owner when idle; separate UI-owned conference participant truth.

## Forward refactors (deferred to Increment 3+)

* Tenant switching must re-scope/invalidate: `queries`, realtime subscriptions (once WS exists), cached data, route selections, permission scopes — **ADR-002** owns this contract.
* Permission evaluation boundary stays `has(capability)` first, backend authority later — **ADR-005**.
* No mega-store: prefer domain boundaries `telephony/`, `assist/`, `social/`, `tenant/`, `auth/`, `monitoring/`, `reports/` — compatible with current `features/*` + `domain/*` layout (§26).

## Gaps recorded (not fixed in Increment 1)

* Tenant context is authoritative but non-scoping — see `FLEX_HARDENING_BASELINE.md` §4 and ADR-002.
* Permission model is UI-only — `auth/capabilities.tsx:8-13` deferred; backend authority NOT VERIFIED.
* Realtime is polling-only — staleness is UI-only without reconnect semantics — see `FLEX_REALTIME_CHANNEL_AUDIT.md`.
