# FLEX Automation Capability Audit — Increment A7 (deferred)

No generic automation engine at baseline — backend has no workflow rules (§121). Deferred automations (only if runtime later supports `trigger→conditions→action→audit→idempotency`): follow-up after missed call, flag aging callback, escalate stale Social. AI targets (call summary, suggested next steps) remain call-scoped, Assist non-global, privacy-reviewed, never blocking core workflow (§§126–132). Not shipped in this POC.
