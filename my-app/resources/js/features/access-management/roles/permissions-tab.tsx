import React from 'react';

export function PermissionsTab() {
    return (
        <div className="flex flex-col gap-[var(--flex-space-section)] w-full">
            <p className="text-sm text-flex-text-muted">Permissions directory — coming next.</p>
        </div>
    );
}
