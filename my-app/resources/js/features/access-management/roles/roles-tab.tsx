import React from 'react';

export function RolesTab() {
    return (
        <div className="flex flex-col gap-[var(--flex-space-section)] w-full">
            <p className="text-sm text-flex-text-muted">Roles directory — coming next.</p>
        </div>
    );
}
