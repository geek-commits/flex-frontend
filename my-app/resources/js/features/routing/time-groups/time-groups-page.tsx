import React from 'react';
import { RoutingShell } from '@/features/routing/routing-shell';

export function TimeGroupsPage() {
    return (
        <RoutingShell title="Time Groups" subtitle="Define reusable schedules for time-based routing.">
            <p className="text-xs text-flex-text-muted">Time Group directory — coming next.</p>
        </RoutingShell>
    );
}
