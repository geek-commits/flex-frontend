import React from 'react';
import { RoutingShell } from '@/features/routing/routing-shell';

export function TimeConditionsPage() {
    return (
        <RoutingShell title="Time Conditions" subtitle="Route calls by date, time, and schedule.">
            <p className="text-xs text-flex-text-muted">Time Condition directory — coming next.</p>
        </RoutingShell>
    );
}
